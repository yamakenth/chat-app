import { Chat, Message, User } from "./serverResBody";
import { WsClientToServerEvents, WsServerToClientEvents } from "./socket";

export type {
  Chat,
  Message,
  User,
  WsClientToServerEvents,
  WsServerToClientEvents,
};
